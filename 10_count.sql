SELECT COUNT(*) FROM users;
--Counts number of users inside the table.

SELECT COUNT(age) FROM users;
--Counts number of users who have a determined age, without counting the ones marked with null information.
