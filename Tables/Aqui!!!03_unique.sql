CREATE TABLE persons2 (
    id int NOT NULL,
    name varchar(100) NOT NULL,
    age int,
    email varchar(50),
    created date
  ):
--This is the command for creating a table named "persons2" inside a new created data base. The string values are specified. "NOT NULL" forces the user to enter a specific value.
