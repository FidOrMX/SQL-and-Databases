SELECT * FROM users
"Selects the whole table users."
"Remark: one has to always finish with (;) or else the code will not function, except for the case in which the whole table has to be selected."
  
SELECT name FROM users;
"Selects the columns name from table users."

SELECT user_id, name FROM users;
"Selects column user_id from table users."
