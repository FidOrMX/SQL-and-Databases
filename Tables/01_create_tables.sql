CREATE TABLE persons (
    id int,
    name varchar(100),
    age int,
    email varchar(50),
    created date
  ):
--This is the command for creating a table named "persons" inside a new created data base. The string values are specified.
